<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── 权限声明 ── -->

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 通知权限（Android 13+ 必须申请） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 开机自启 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 无障碍服务权限（绑定AccessibilityService必须） -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- 震动反馈（可选） -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        注意：SYSTEM_ALERT_WINDOW（悬浮窗权限）已移除
        原悬浮窗方案已替换为小米超级岛原生通知接口（miui.focus.param）
        超级岛功能通过标准通知实现，无需悬浮窗权限
    -->

    <application
        android:name=".PickCodeApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PickCode"
        tools:targetApi="31">

        <!-- 主界面 -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 截屏识别 Activity（已弃用，保留声明防止类引用报错，当前版本不再调用） -->
        <activity
            android:name=".ui.activity.CaptureActivity"
            android:exported="false"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance" />

        <!-- 权限请求透明 Activity（截屏权限申请专用，保留兼容） -->
        <activity
            android:name=".ui.activity.PermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.Transparent"
            android:launchMode="singleInstance" />

        <!-- 设置界面 -->
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity"
            android:theme="@style/Theme.PickCode.Settings" />

        <!-- 关于页面 -->
        <activity
            android:name=".ui.activity.AboutActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity"
            android:theme="@style/Theme.PickCode.About" />

        <!-- 运行日志查看器 -->
        <activity
            android:name=".ui.activity.LogViewerActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainActivity" />

        <!-- 前台服务：常驻通知保活 + 节点树提取中转 + 标准通知展示 -->
        <!--
            Android 14 (API 34) 限制：
            - 必须声明 foregroundServiceType
            - specialUse 类型需要 FOREGROUND_SERVICE_SPECIAL_USE 权限和 subtype 说明
        -->
        <service
            android:name=".service.PickCodeService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="用户主动点击通知或快捷磁贴时，维持前台服务以触发本地无障碍节点文字提取并展示验证码通知。" />
        </service>

        <!-- Quick Settings Tile（快捷开关） -->
        <service
            android:name=".tile.PickCodeTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 开机自启广播 -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
            复制验证码广播接收器（通知 Action 按钮触发）
            仅接收应用内显式 PendingIntent 广播。
        -->
        <receiver
            android:name=".service.CopyCodeReceiver"
            android:exported="false" />

        <!-- 无障碍服务：核心 — 节点树文字提取取件码（效仿 Tally 记账App） -->
        <service
            android:name=".service.PickCodeAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
