<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.PickCode" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent_primary</item>
        <item name="colorPrimaryVariant">@color/accent_light</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/bg_primary</item>
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.PickCode.About" parent="Theme.PickCode">
        <!-- 状态栏透明，让 AppBar 沉浸进去 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- 深色 Hero 背景 → 状态栏图标用浅色 -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.PickCode.Settings" parent="Theme.PickCode">
        <!-- 状态栏透明，让 AppBar 沉浸进去 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- 浅色工具栏背景 → 状态栏图标保持深色 -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 透明 Activity（用于截屏权限弹窗） -->
    <style name="Theme.Transparent" parent="Theme.PickCode">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="TextAppearance.PickCode.Title" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">22sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.PickCode.TabText" parent="TextAppearance.MaterialComponents.Button">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>
</resources>
